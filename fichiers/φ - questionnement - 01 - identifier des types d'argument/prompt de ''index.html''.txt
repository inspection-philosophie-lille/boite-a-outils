Créez un fichier HTML nommé "index.html" pour une application web interactive intitulée "identifier le type d'arguments". L'objectif est de permettre à l'utilisateur d'associer des exemples (noms d'arguments) à leurs définitions correspondantes en traçant des flèches entre deux colonnes de cellules dans une interface responsive. Voici les exigences détaillées :

1. **Structure HTML** :
   - Utilisez un `<!DOCTYPE html>` avec un encodage UTF-8 et un viewport pour la responsivité (`width=device-width, initial-scale=1.0`).
   - Incluez un favicon (`../../src/favicon.ico`).
   - Ajoutez un titre : "identifier le type d'arguments".
   - Structurez le corps (`body`) avec :
     - Un paragraphe `<p>` avec la classe `title` contenant "identifier le type d'arguments".
     - Un paragraphe `<p>` avec la classe `subtitle` contenant "associez chaque exemple au nom de l'argument correspondant".
     - Un conteneur `<div>` avec la classe `container` contenant :
       - Une colonne gauche `<div class="column left">` avec une table `<table id="repereTable">`.
       - Une colonne droite `<div class="column right">` avec une table `<table id="definitionTable">`.
       - Un élément `<canvas id="lineCanvas">` pour dessiner les flèches.
     - Un bouton `<button id="actionButton" onclick="performAction()" disabled>` avec le texte initial "Vérifier".
   - Incluez deux scripts :
     - `<script src="arguments.js">` pour les données des arguments.
     - Un script inline pour la gestion du fichier Excel (identique au code fourni pour `loadFileData`).
     - Un script inline principal pour la logique de l'application.

2. **Styles CSS** :
   - Appliquez exactement les styles CSS fournis dans le fichier HTML de référence (artifact version ID: 6739d5a6-f15b-4b18-8f19-ba0d50e82646), incluant :
     - Styles globaux (`box-sizing: border-box`, `font-family: 'Muli', Arial, sans-serif`, etc.).
     - Styles pour `.title`, `.subtitle`, `.container`, `.column`, `#repereTable`, `#definitionTable`, `td`, `canvas`, `button`, et `.reload-icon`.
     - Media queries pour `max-width: 768px` et `max-width: 480px` pour la responsivité.
   - Assurez-vous que le layout passe en colonnes verticales en mode portable (`max-width: 768px`) avec des tables à 100% de largeur, un défilement vertical (`max-height: 35vh` pour `#repereTable`, `40vh` pour `#definitionTable`), et un bouton sticky en bas.

3. **JavaScript** :
   - **Données** : Supposez que `arguments.js` fournit un tableau `argumentsData` contenant des objets avec `name` et `definition` (par exemple, `{ name: "Argument A", definition: "Définition 1" }`).
   - **Fonctionnalités générales** :
     - Chargez 5 paires aléatoires depuis `argumentsData` dans `repereTable` (noms) et `definitionTable` (définitions), mélangées aléatoirement.
     - Permettez à l'utilisateur de cliquer sur une cellule de `repereTable` et une cellule de `definitionTable` pour tracer une flèche entre elles.
     - Empêchez la sélection de cellules déjà utilisées (via `usedCells` Set).
     - Activez le bouton "Vérifier" uniquement lorsque 5 liens sont tracés (`links.length === 5`).
     - Implémentez les actions du bouton : "Vérifier" (colorie les flèches en vert/rouge selon la correction), "Corriger" (montre les flèches correctes), "Recharger" (réinitialise avec un nouvel ensemble aléatoire).
   - **Tracé des flèches** :
     - **Mode portable (`window.innerWidth <= 768px`)** :
       - Les flèches doivent partir du **bord bas** de la cellule de `repereTable` (`rect.bottom - 5`, 5px à l'intérieur) et arriver au **bord haut** de la cellule de `definitionTable` (`rect.top + 5`, 5px à l'intérieur), quel que soit l'ordre de sélection (`repereTable` → `definitionTable` ou l'inverse).
       - Les coordonnées horizontales (`startX`, `endX`) doivent être alignées en utilisant `repereIndex` (basé sur `cell.dataset.index` de `repereTable`) avec la formule `rect.left + (rect.width * (repereIndex + 0.5) / 5)` pour éviter le chevauchement des flèches, même pour des noms d'arguments identiques.
     - **Mode desktop (`window.innerWidth > 768px`)** :
       - Les flèches partent du bord droit de `repereTable` (`rect.right - 5`) et arrivent au bord gauche de `definitionTable` (`rect.left + 5`), centrées verticalement (`rect.top + rect.height / 2`).
   - **Fonctions spécifiques** :
     - `resizeCanvas` : Ajuste la taille du canvas à celle du conteneur.
     - `isVerticalLayout` : Retourne `true` si `window.innerWidth <= 768`.
     - `getRandomElements` : Sélectionne `count` éléments aléatoires d'un tableau.
     - `loadReperes` : Charge 5 paires aléatoires dans les tables, attribue `dataset.index` (0 à 4).
     - `selectCell(event, type, info)` : Gère la sélection des cellules et le tracé des flèches, en respectant l'orientation bas → haut en mode portable.
     - `drawLink(startX, startY, endX, endY, color)` : Dessine une flèche avec une pointe (longueur 10px) et une largeur de 2px.
     - `resetDrawing` : Réinitialise l'état de dessin.
     - `performAction` : Gère les actions du bouton ("check", "correct", "reload").
     - `checkLinks` : Vérifie les liens, colorie les flèches (vert pour correct, rouge pour incorrect).
     - `correctLinks` : Dessine les flèches correctes (vertes) en utilisant les mêmes points d'ancrage que `selectCell`.
     - `reload` : Réinitialise l'application.
     - `addEventListeners` : Ajoute les écouteurs de clics aux cellules.
   - Initialisez l'application dans `window.onload` en chargeant les données, ajustant le canvas, et ajoutant les écouteurs.

4. **Comportement du bouton** :
   - Désactivé par défaut (`disabled`) jusqu'à ce que 5 liens soient tracés.
   - Texte : "Vérifier" (initial), "Corriger" (après vérification), icône de rechargement (`&#8635;`) pour recharger.
   - Style sticky en mode portable (`position: sticky; bottom: 0.5rem`).

5. **Exigences supplémentaires** :
   - Les flèches doivent être dessinées sur un canvas superposé (`position: absolute`, `pointer-events: none`, `z-index: 1`).
   - Les cellules doivent être réactives (pas de réutilisation via `usedCells`).
   - Le code doit être robuste, avec des vérifications pour `argumentsData` vide ou mal formé.
   - Incluez le script `loadFileData` pour la compatibilité Excel, même s'il n'est pas utilisé (gardez-le identique au code fourni).

6. **Sortie** :
   - Fournissez le code HTML complet, incluant les sections `<style>` et `<script>`, correspondant exactement au fichier de référence (artifact version ID: 6739d5a6-f15b-4b18-8f19-ba0d50e82646).
   - Assurez-vous que la logique des flèches en mode portable (bas de `repereTable` → haut de `definitionTable`) est strictement respectée.

Merci de produire le fichier HTML complet avec tous les détails ci-dessus.