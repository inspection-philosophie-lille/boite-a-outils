ute la classe correct si correct, incorrect sinon.
Rend les trous incorrects déplaçables (draggable=true) avec écouteurs dragover et drop.
Retourne true si tous les trous sont corrects, false sinon.


Fonction correctAnswers() :

Remplit chaque trou avec son data-word et ajoute la classe correct.


Écouteur pour #actionButton :

Si le texte est "vérifier", appelle verifyAnswers() et change le texte en "corriger".
Si le texte est "corriger", appelle correctAnswers() et change le texte en "recommencer".
Si le texte est "recommencer", appelle initializeExercise() et change le texte en "vérifier".


Initialisation : Appeler initializeExercise() au chargement.

Contraintes visuelles :

L'interlignage doit être suffisant et constant (même avec des trous provoquant des retours à la ligne).
Les trous ne doivent pas se toucher verticalement sur des lignes consécutives.
Les trous doivent être alignés verticalement avec le texte (vertical-align: middle).
La zone de définition (.text-container) doit occuper la largeur maximale avec des marges minimales.
Le bouton #actionButton doit rester visible sans défilement, même avec des définitions longues.

Contraintes supplémentaires :

Le code doit être responsive avec des tailles ajustées via clamp() pour desktop, mobile (max-width: 600px), et grands écrans (min-width: 1200px).
Le style doit utiliser un design néomorphique cohérent.
Le texte des titres et du bouton doit être en minuscules.
Ajouter un espace insécable après le mot à définir dans la définition.
Éviter tout bug de remplacement (par exemple, fragments HTML comme "" style="min-width: 130px;">") en utilisant un échappement correct des caractères spéciaux dans initializeExercise().

Générez le fichier index.html avec le HTML, CSS, et JavaScript correspondant exactement à ces spécifications.